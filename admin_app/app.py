"""
DEPRECATED: API endpoints were merged into `admin_app/app/main.py`.

This file is kept for backward-compatibility in the source tree but the
implementation now lives in `admin_app/app/main.py` which serves both the
web UI and the API (routes under `/admin` and `/admin/api/*`).
"""
